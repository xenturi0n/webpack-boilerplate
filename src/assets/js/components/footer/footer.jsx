import React, {Component}from 'react';
import styles from './estilos.css';

const Footer = (props) => {
    return(
        <h1 className={styles.footer + ' una-clase-perrona'}>Esto es un footer</h1>
    );
}

export default Footer;
