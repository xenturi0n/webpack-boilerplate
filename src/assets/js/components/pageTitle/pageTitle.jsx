import React, {Component}from 'react';
import styles from './pageTitle.css';

const PageTitle = (props) => {
    return(
        <h1 className={styles.pageTitle}>Hola Esto funciona</h1>
    );
}

export default PageTitle;
